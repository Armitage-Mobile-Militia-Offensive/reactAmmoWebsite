import React, {Component} from 'react';

import ClockUTC from '../ClockUTC'

class Scheduler extends Component {
  render() {
    return (
      <div style={{paddingTop: '46'}}>
        <h1>Welcome to AMMO's calendar view</h1>
     </div>
    );
  }
}

export default Scheduler;
